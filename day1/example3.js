// 比较操作符
var a = false;
var b = "";
if(a === b) {
    alert("a equals b");
}
// ===符号意味着严格比较，而==符号会将false与空字符划上等号
// 同理，严格不相等可表示为 !==

// 逻辑操作符
var c = 7;
if(c <= 8 && c >= 6) {
    alert("C is 7 (?");
}